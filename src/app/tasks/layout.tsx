import { Metadata } from 'next';
import TasksPage from './page';

export const metadata: Metadata = {
  title: 'Tasks - Etherana SX',
  description: 'Recurring tasks and reminders for solo entrepreneurs.',
};

export default TasksPage;
